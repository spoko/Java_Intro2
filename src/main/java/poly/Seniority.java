package poly;

public interface Seniority {
    //methods in interface without body
    void supportSomeone(); //public by default, and cannot be anything else!
    String giveFeedback(); //public by default, and cannot be anything else!
}
